"""Textual TUI entrypoint placeholder.

Not implemented yet. When it lands, it depends only on `ports_in/` via
`container.py`, mirroring `cli/` — no changes to `application/` or `adapters/`
should be required to add it.
"""
